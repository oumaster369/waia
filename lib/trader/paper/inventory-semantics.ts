/** PR1 canonical SPOT inventory derivation semantics. */
export const INVENTORY_SEMANTICS_VERSION = "waia.trader.inventory.v1" as const;

export type InventorySemanticsVersion = typeof INVENTORY_SEMANTICS_VERSION;
