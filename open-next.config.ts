import { defineCloudflareConfig } from "@opennextjs/cloudflare";

/** OpenNext Cloudflare adapter config. R2 incremental cache can be added later (see OpenNext caching docs). */
export default defineCloudflareConfig({});
